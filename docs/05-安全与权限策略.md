# Claude Code 安全与权限策略

> AI 能力越强，安全越重要。以下分析 Claude Code 如何实现"既强大又可控"的安全体系。

## 一、安全架构总览

Claude Code 采用**六层纵深防御**，每一层独立工作，任何一层都能阻止危险操作：

```mermaid
graph TB
    REQ["工具调用请求"] --> L1
    
    subgraph L1["第 1 层: 权限规则"]
        R1["匹配 Allow/Deny/Ask 规则"]
    end
    
    L1 --> L2
    subgraph L2["第 2 层: Hook 检查"]
        R2["用户自定义脚本拦截"]
    end
    
    L2 --> L3
    subgraph L3["第 3 层: AI 分类器"]
        R3["AI 自动评估风险"]
    end
    
    L3 --> L4
    subgraph L4["第 4 层: 沙箱隔离"]
        R4["文件/网络/进程限制"]
    end
    
    L4 --> L5
    subgraph L5["第 5 层: 用户确认"]
        R5["UI 弹框请求批准"]
    end
    
    L5 --> L6
    subgraph L6["第 6 层: 企业策略"]
        R6["远程管理覆盖一切"]
    end
    
    L6 --> EXEC["执行工具"]

    style L1 fill:#e8f5e9
    style L2 fill:#e3f2fd
    style L3 fill:#fff3e0
    style L4 fill:#fce4ec
    style L5 fill:#f3e5f5
    style L6 fill:#ffebee
```

---

## 二、权限模式

Claude Code 定义了 **6 种权限模式**，形成安全性梯度：

| 模式 | 行为 | 风险 | 适用场景 |
|------|------|------|---------|
| **default** | 每个操作都问用户 | 最低 | 首次使用、敏感项目 |
| **acceptEdits** | 自动接受文件编辑，其他询问 | 低-中 | 日常开发 |
| **plan** | 只看不做，展示计划 | 低 | 审查方案 |
| **auto** | AI 分类器自动决策 | 中 | 高效工作 |
| **bypassPermissions** | 全部自动允许 | 高 | 受信环境 |
| **dontAsk** | 全部自动拒绝 | — | 完全隔离 |

```mermaid
graph LR
    SAFE["安全 ←"] --- DEFAULT["default<br/>逐一确认"]
    DEFAULT --- ACCEPT["acceptEdits<br/>自动编辑"]
    ACCEPT --- PLAN["plan<br/>只看不做"]
    PLAN --- AUTO["auto<br/>AI 决策"]
    AUTO --- BYPASS["bypass<br/>全部允许"]
    BYPASS --- FAST["→ 高效"]

    style DEFAULT fill:#e8f5e9
    style AUTO fill:#fff3e0
    style BYPASS fill:#ffebee
```

---

## 三、权限规则系统

### 3.1 规则三要素

每条权限规则由三个部分组成：

```
规则 = 来源 + 行为 + 匹配条件

示例:
  ┌──────────────┬─────────┬──────────────────────┐
  │ 来源          │ 行为     │ 匹配条件              │
  ├──────────────┼─────────┼──────────────────────┤
  │ userSettings │ allow   │ Bash(git *)          │
  │ projectSettings │ deny │ Bash(sudo:*)         │
  │ session      │ ask     │ FileEdit(package.json)│
  └──────────────┴─────────┴──────────────────────┘
```

### 3.2 规则来源优先级

优先级从低到高排列，**高优先级的规则覆盖低优先级**：

```mermaid
graph BT
    CLI["命令行参数<br/>--bash-allow"] --> SESSION["会话临时规则"]
    SESSION --> PROJECT["项目配置<br/>.claude/settings.json"]
    PROJECT --> USER["用户配置<br/>~/.claude/settings.json"]
    USER --> MDM["MDM 企业管理"]
    MDM --> REMOTE["远程管理策略<br/>（最高优先级）"]

    style REMOTE fill:#ffebee,stroke:#c62828
    style MDM fill:#fce4ec
    style USER fill:#f3e5f5
```

### 3.3 同优先级内的决策顺序

```
同一优先级内:
  Deny 规则 > Ask 规则 > Allow 规则

也就是说，"拒绝"总是优先于"允许"。
```

### 3.4 规则匹配语法

```
工具级:       Bash                     → 匹配所有 Bash 命令
前缀匹配:     Bash(git:*)              → 匹配 git 开头的命令
通配符:       Bash(npm *)              → 匹配 npm 加任何参数
MCP 服务器:   mcp__github              → 匹配整个 GitHub MCP
MCP 工具:     mcp__github__create_issue → 匹配特定 MCP 工具
文件路径:     FileEdit(./src/**)       → 匹配 src 目录下的编辑
```

---

## 四、权限检查完整流程

```mermaid
flowchart TD
    START["工具调用请求"] --> DENY{"Deny 规则<br/>匹配？"}
    
    DENY -->|"是"| BLOCKED["❌ 拒绝执行"]
    DENY -->|"否"| ALLOW{"Allow 规则<br/>匹配？"}
    
    ALLOW -->|"是"| EXEC["✅ 直接执行"]
    ALLOW -->|"否"| ASK{"Ask 规则<br/>匹配？"}
    
    ASK -->|"是"| PROMPT["📋 进入确认流程"]
    ASK -->|"否"| HOOK{"Hook 检查"}
    
    HOOK -->|"hook 返回 deny"| BLOCKED
    HOOK -->|"hook 返回 allow"| EXEC
    HOOK -->|"hook 返回 ask"| PROMPT
    HOOK -->|"无 hook"| CLASSIFIER{"AI 分类器<br/>(auto 模式)"}
    
    CLASSIFIER -->|"安全"| EXEC
    CLASSIFIER -->|"不确定"| PROMPT
    CLASSIFIER -->|"危险"| BLOCKED
    
    PROMPT --> USER{"用户决策"}
    USER -->|"允许"| EXEC
    USER -->|"拒绝"| BLOCKED
    USER -->|"总是允许"| SAVE["保存为 Allow 规则"]
    SAVE --> EXEC
```

---

## 五、沙箱机制

沙箱通过**四个维度**隔离 Agent 的执行环境：

```mermaid
graph TB
    subgraph FS["📁 文件系统隔离"]
        FS1["allowRead: ['/project/**']"]
        FS2["allowWrite: ['/tmp/claude/**']"]
        FS3["denyWrite: ['~/.ssh/**']"]
    end

    subgraph NET["🌐 网络隔离"]
        NET1["allowlist 模式: 仅指定域名"]
        NET2["denylist 模式: 除指定外全允许"]
        NET3["managedDomainsOnly: 企业白名单"]
    end

    subgraph PROC["⚙️ 进程隔离"]
        PROC1["禁止 sudo"]
        PROC2["禁止 ssh"]
        PROC3["禁止 eval/exec"]
    end

    subgraph ENV["🔑 环境变量隔离"]
        ENV1["清理 AWS_* 凭证"]
        ENV2["清理 ANTHROPIC_* 密钥"]
        ENV3["清理 GITHUB_* Token"]
        ENV4["保留 PATH, HOME, LANG"]
    end
```

### 文件路径配置语法

```
/path         → 相对于设置文件目录
//path        → 绝对路径（双斜杠）
~/path        → 用户主目录
./path        → 当前工作目录

特殊规则:
  ! 前缀      → 在允许列表中创建例外（deny-within-allow）
  
例子:
  allowWrite: ["/tmp/**"]           → 允许写 /tmp
  denyWrite:  ["!${SETTINGS_DIR}"]  → 但禁止写设置目录
```

### 安全防护

```
Symlink 防护:  realpath() 解析所有符号链接，防止"链接逃逸"
路径穿越防护:  ../../../etc/passwd → 直接拒绝
Glob 展开:    /path/*/config.* → 每个匹配路径都单独验证
输出重定向:    echo "data" > file → 检查 file 的写权限
```

---

## 六、AI 分类器 (Auto 模式)

在 Auto 模式下，AI 分类器自动判断工具调用是否安全：

### 安全白名单（不需要分类）

```
自动放行的工具:
├─ FileRead      ← 只读
├─ Grep, Glob    ← 搜索
├─ TodoWrite     ← 元数据
├─ AskUserQuestion ← 交互
├─ Sleep         ← 无副作用
└─ 其他只读工具
```

### 分类器工作原理

```mermaid
flowchart LR
    CMD["Bash: npm run build"] --> WL{"在安全白名单？"}
    WL -->|"是"| SAFE["✅ 自动允许"]
    WL -->|"否"| CLS["AI 分类器评估"]
    
    CLS --> RULES["匹配三层规则"]
    RULES --> R1["Allow 规则 → 允许"]
    RULES --> R2["Soft Deny → 警告但允许"]
    RULES --> R3["环境条件 → 条件判断"]
    
    CLS --> RESULT{"评估结果"}
    RESULT -->|"安全"| SAFE
    RESULT -->|"需确认"| ASK["询问用户"]
    RESULT -->|"危险"| DENY["拒绝"]
```

### 危险命令模式检测

进入 Auto 模式时，以下类型的权限规则会被**自动移除**（因为它们太宽泛）：

```
自动移除的危险规则:
├─ python:*    ← 可执行任意 Python
├─ node:*      ← 可执行任意 JS
├─ sh:* / bash:* ← Shell 脚本
├─ eval:*      ← 代码注入风险
├─ ssh:*       ← 远程访问
├─ sudo:*      ← 权限提升
└─ curl:*      ← 网络请求（内部环境）
```

---

## 七、Hook 安全检查

Hook 允许用户在权限决策链中插入自定义逻辑：

```mermaid
sequenceDiagram
    participant 系统
    participant Hook脚本
    participant 用户

    Note over 系统: 工具调用到达
    系统->>Hook脚本: toolPermissionHook(context)
    
    alt Hook 返回 allow
        Hook脚本-->>系统: { allow: true }
        Note over 系统: 跳过后续检查，直接执行
    else Hook 返回 deny
        Hook脚本-->>系统: { deny: true, message: "被阻止" }
        Note over 系统: 拒绝，返回错误消息
    else Hook 返回 ask
        Hook脚本-->>系统: { ask: true }
        系统->>用户: 显示确认对话框
    else 无返回值
        Note over 系统: 继续正常权限流程
    end
```

### Hook 类型

| Hook 类型 | 执行方式 | 用途 |
|-----------|---------|------|
| **Command** | Bash/Shell 脚本 | 退出码控制（0=允许，2=阻止） |
| **Prompt** | LLM 生成响应 | 自然语言决策 |
| **Agent** | 完整 AI Agent | 多轮推理决策 |
| **HTTP** | REST API 调用 | 对接外部审计系统 |
| **Function** | TypeScript 回调 | SDK 集成 |

### Hook 安全隔离

```
Hook 的限制:
├─ 超时保护: 5 秒超时
├─ 无限循环防护: 检测死循环
├─ 隔离执行: 无法访问凭证和设置
└─ 超时默认: 超时 → 默认允许（fail-open）
```

---

## 八、信任机制

### 首次运行的信任对话

当在新项目目录首次运行时，系统会评估风险并提示：

```mermaid
flowchart TD
    START["首次运行"] --> SCAN["扫描项目配置"]
    
    SCAN --> RISK["计算风险分数"]
    
    RISK --> ITEMS["检查风险项"]
    ITEMS --> I1["MCP 服务器 (+15分)"]
    ITEMS --> I2["自定义 Hook (+20分)"]
    ITEMS --> I3["Bash 权限 (+15分)"]
    ITEMS --> I4["API Key Helper (+10分)"]
    ITEMS --> I5["AWS/GCP 命令 (+10分)"]
    ITEMS --> I6["环境变量重写 (+5分)"]
    
    I1 & I2 & I3 & I4 & I5 & I6 --> TOTAL{"总分"}
    
    TOTAL -->|"> 70"| FORCE["强制信任确认"]
    TOTAL -->|"30-70"| INFO["信息提示"]
    TOTAL -->|"< 30"| AUTO["自动信任"]
```

### 信任验证

信任状态保存在 `.claude/trust_config.json` 中，包含配置哈希，防止被篡改。

---

## 九、企业级安全管控

### 远程管理设置

```mermaid
graph TB
    IT["企业 IT 管理员"] --> CLOUD["Cloud Management API"]
    CLOUD --> CC["Claude Code (客户端)"]
    
    CC --> FETCH["初始拉取"]
    CC --> POLL["后台轮询 (每小时)"]
    CC --> CACHE["ETag 缓存"]
    
    subgraph 优先级["设置优先级 (从低到高)"]
        L["本地设置"]
        P["项目设置"]
        M["MDM 设置"]
        R["远程管理 (最高)"]
    end

    style R fill:#ffebee,stroke:#c62828
```

### 企业可控制的内容

| 控制项 | 示例 | 说明 |
|--------|------|------|
| **工具白/黑名单** | `alwaysDeny: ["sudo:*"]` | 禁止特定命令 |
| **文件访问** | `allowRead: ["/data/**"]` | 限制可读路径 |
| **网络限制** | `allowManagedDomainsOnly: true` | 仅允许企业域名 |
| **禁用功能** | `disabledFeatures: ["bypassPermissionsMode"]` | 禁用绕过模式 |
| **禁用工具** | `disabledTools: ["Agent"]` | 禁用子 Agent |
| **强制 MFA** | `requiresMFA: true` | 强制多因素认证 |

### 故障模式

```
网络不可用时的策略:
├─ 远程设置: 使用缓存（fail-open）
├─ 策略限制: 禁用受限功能
└─ 确保: 永远不会因网络问题阻断工作
```

---

## 十、认证与凭据安全

### 认证来源优先级

```
1. 环境变量     ANTHROPIC_API_KEY
2. Key Helper   settings.json → credentials.apiKey.helper
3. 配置文件     ~/.claude/config.json
4. OAuth Token  Claude.ai 登录
5. Keychain     macOS 钥匙链 (优先) / Linux 文件 (降级)
```

### 凭据安全存储

```mermaid
graph TB
    subgraph macOS["macOS: Keychain 存储"]
        KW["写入: security -i<br/>十六进制编码"]
        KR["读取: security find-generic-password<br/>5分钟缓存"]
        KG["竞态保护: generation 字段"]
    end

    subgraph Linux["Linux: 降级存储"]
        PT["明文文件: ~/.claude/credentials<br/>权限: 600 (仅所有者)"]
    end

    subgraph OAuth["OAuth 认证"]
        PKCE["PKCE 保护<br/>防中间人"]
        REFRESH["自动刷新<br/>过期前5分钟"]
        SCOPE["最小 Scope<br/>仅必要权限"]
    end
```

### 凭据安全措施

| 措施 | 实现 | 目的 |
|------|------|------|
| **加密存储** | macOS Keychain | 系统级保护 |
| **TTL 缓存** | 5分钟过期 | 降低泄漏窗口 |
| **Stale-while-error** | 读取失败用旧缓存 | 避免误登出 |
| **日志脱敏** | 掩码输出 | 防日志泄漏 |
| **PKCE 保护** | OAuth code 交换 | 防中间人攻击 |
| **原子写入** | write + rename | 防文件损坏 |

---

## 十一、权限监控与审计

### 每个权限决策都被记录

```json
{
  "toolName": "Bash",
  "decision": "allow",
  "decisionSource": "rule",
  "reason": "Matched allow rule: Bash(git *)",
  "durationMs": 2,
  "tokenUsage": { "input": 0, "output": 0 }
}
```

### 拒绝追踪与熔断

```
如果 1 分钟内连续拒绝 > 10 次:
  → 自动切换到交互模式（强制显示 UI）
  → 防止 AI 在循环中反复尝试被拒绝的操作
```

---

## 十二、设计亮点总结

| 设计点 | 做法 | 为什么 |
|--------|------|--------|
| **六层纵深** | 规则→Hook→分类器→沙箱→UI→企业 | 任何一层失败都不会导致安全事故 |
| **Deny 优先** | 同级别内 deny > ask > allow | 安全始终优先 |
| **Fail-closed** | 默认 isReadOnly=false | 未声明的能力默认受限 |
| **企业覆盖** | 远程策略优先级最高 | IT 可控制一切 |
| **风险评分** | 自动检测配置风险 | 首次运行时提醒用户 |
| **不可变上下文** | 权限上下文每次更新返回新对象 | 防止竞态条件 |
| **缓存安全** | Stale-while-error | 安全不因临时故障中断 |
| **最小权限** | 子 Agent 工具受限 | 能力越大，限制越多 |
